<?php

return array(
    'STRIPE_SECRET_KEY' => 'sk_test_your_secret_key_here',
    'STRIPE_CONNECT_COUNTRY' => 'IE',
    'STRIPE_IDENTITY_WEBHOOK_SECRET' => 'whsec_your_identity_webhook_secret_here',

    // Public site URL for links in emails (password reset, etc.). No trailing slash.
    // 'APP_BASE_URL' => 'https://anytransport.ie',

    // All admin-panel notifications go here (verification queue, profile changes, listing reports, etc.).
    // If omitted, defaults to admin@anytransport.ie
    // 'ADMIN_EMAIL' => 'admin@anytransport.ie',

    // Outbound email (required for password reset and provider emails). If SMTP_HOST is set, PHP mail() is not used.
    //
    // Namecheap Private Email: create the mailbox in Namecheap → Private Email, then use:
    // SMTP_USER = full email address; SMTP_PASS = that mailbox’s password.
    // Set SMTP_EHLO_DOMAIN to your bare domain (e.g. anytransport.ie) if the server rejects the sender.
    //
    // 'SMTP_HOST' => 'mail.privateemail.com',
    // 'SMTP_PORT' => 587,
    // 'SMTP_USER' => 'no-reply@anytransport.ie',
    // 'SMTP_PASS' => '6LhYqDNDka#',
    // 'SMTP_SECURE' => 'tls',
    // 'SMTP_FROM' => 'no-reply@anytransport.ie',
    // 'SMTP_EHLO_DOMAIN' => 'anytransport.ie',
);